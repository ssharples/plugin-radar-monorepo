// Placeholder for LenisProvider
// This component was removed but is still referenced in app/layout.tsx
export function LenisProvider({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
