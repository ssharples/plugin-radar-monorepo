#!/bin/bash
claude -p "Rebuild the JUCE AU plugin target, package it, install to ~/Library/Audio/Plug-Ins/Components/, and verify the installation. Report any build errors." --allowedTools "Edit,Read,Bash,Grep"
